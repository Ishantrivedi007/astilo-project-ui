# Install this in addition to requirements.txt when DATABASE_URL points at Postgres.
# psycopg (v3) is used instead of psycopg2 — psycopg2-binary has no precompiled
# wheel yet for newer Python versions (e.g. 3.13+/3.14) and fails to build from source.
psycopg[binary]==3.3.5
